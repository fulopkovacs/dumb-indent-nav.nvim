# dumb-indent-nav.nvim

Minimal Lua Neovim plugin.

## Usage

```lua
{
  "dumb-indent-nav.nvim",
  dev = true,
  config = function()
    require("dumb-indent-nav").setup()
  end,
}
```

Then run:

```vim
:DumbIndentNavHello
```
